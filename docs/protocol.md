# Protocol

M-MCP is a contract-first runtime.

The protocol is composed of:

- capability metadata
- context envelopes
- execution context
- execution policies
- traces

The goal is to make capabilities composable without assuming a server process.
